让我们关注 @src/security 下的一些功能

这个目录下的代码的作用是根据不同的编程语言，设定允许的系统调用列表，而后编译成bpf字节码最后编译成动态链接库。

在 @src/runtime 下定义了不同语言的runtime，动态链接库会在调用runtime运行代码之前注入到runtime子进程中（目前未实现）

请你检查 @src/security

1. 是否有重复逻辑
2. 是否可以做简化
3. 是否能正常编译成动态链接库？
